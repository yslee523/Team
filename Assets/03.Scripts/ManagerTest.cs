using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ManagerTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        var v =  NetworkManager.Instance;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
